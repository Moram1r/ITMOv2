# Нагрузочные проверки

Файл ведёт OpenCode. Обсудите с агентом содержание и проверьте предложенный diff. Все дополнения и исправления поручайте агенту в чате.

| Сценарий | Нагрузка и длительность | Допустимый предел | Что измеряем | Подтверждение |
|---|---|---|---|---|
| Стабильная нагрузка на валидных диффах | warm-up 60с; 5 rps в течение 5 минут | http_req_duration p(95) <= 2.0 с; ошибки < 1% | k6: http_req_duration p(95), http_req_failed | k6 thresholds pass; OBS-1: duration, status; 2 повтора — расхождение p95 <= 10% |
| Пиковая нагрузка (уточнено CoV) | warm-up 30с; ramp 5→20 rps за 30с; плато 30с | p(95) <= 3.0 с; ошибки < 3% | k6: p(95), ошибки; статусы | k6 отчёт + thresholds; OBS-1 duration/status; повторяемость подтверждена |
| Oversize-флуд | 10 rps oversize диффов (20001 симв.) 1 мин | 100% HTTP 413; LLM не вызывается | Доля 413; отсутствие LLM-вызовов | CASE.md API-1; OBS-1: только request_id, duration, status |
| Fault-инъекция таймаутов | 2 rps, 2 мин; LLM >10с | 100% контролируемых OUT-1 ответов; исключений нет | Доля таймаутов; структура OUT-1 | CASE.md REL-1/OUT-1; OBS-1 статус timeout_handled |

Если нагрузочное тестирование пока не требуется, объясните это OpenCode. Попросите записать причину и условие, при котором оно понадобится.

## Как использовали AI

- Строка в [`prompts.md`](prompts.md): P2-CoV.
- Что проверили и исправили сами: проверили полноту сценария «Пиковая нагрузка» по вопросам и источникам; структура файла сохранена.
