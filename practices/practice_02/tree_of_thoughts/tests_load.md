# Нагрузочные проверки

Файл ведёт OpenCode. Обсудите с агентом содержание и проверьте предложенный diff. Все дополнения и исправления поручайте агенту в чате.

| Сценарий | Нагрузка и длительность | Допустимый предел | Что измеряем | Подтверждение |
|---|---|---|---|---|
| Стабильная нагрузка на валидных диффах (3 прогона) | warm-up 60с; 5 rps, 5 мин steady (k6 vus≈5) | http_req_duration p(95) <= 2.0 с; http_req_failed < 1% | k6: p(95), error rate; статусы | 3 повтора: расхождение p95 между запусками <= 10%; OBS-1 duration/status; OUT-1 в ответах |
| Пиковая нагрузка (доп.) | ramp 5→20 rps за 30с; плато 30с | p(95) <= 3.0 с; ошибки < 3% | k6 p(95), ошибки | k6 thresholds; OBS-1 логи статусов |
| Oversize-флуд (API-1) | 10 rps oversize (20001 симв.) 1 мин | 100% HTTP 413; 0 вызовов LLM | Доля 413; внешние вызовы | CASE.md API-1; OBS-1: только request_id, duration, status |
| Fault-инъекция таймаутов (REL-1) | 2 rps, 2 мин; LLM задержка >10с | 100% контролируемых OUT-1 ответов; исключений нет | Таймауты; валидность OUT-1 | CASE.md REL-1/OUT-1; OBS-1 статус timeout_handled |

Если нагрузочное тестирование пока не требуется, объясните это OpenCode. Попросите записать причину и условие, при котором оно понадобится.

## Как использовали AI

- Строка в [`prompts.md`](prompts.md): P2-ToT.
- Что проверили и исправили сами: добавили требование к повторяемости стабильного сценария; структура файла сохранена.
