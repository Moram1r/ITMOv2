# Few-shot

Файл ведёт OpenCode по вашим запросам. Агент записывает фактические результаты экспериментов и вносит изменения в связанные файлы. Свою оценку сообщайте ему в чате; вручную заполнять шаблон не нужно.

- Артефакт Практики 1: `tests_load.md`
- Что хотим улучшить: сделать сценарии воспроизводимыми и однозначно проверяемыми: добавить warm-up/ramp-up, зафиксировать инструменты (k6), метрики (p95, error rate), и явные проверки соответствия CASE.md (API-1, REL-1, OUT-1, OBS-1).

## Примеры

### Хороший результат

Стабильная нагрузка на валидных диффах | 5 rps, 1 мин warm-up + 5 мин steady | p95 <= 2.0 с; error rate < 1% | k6 Trend p(95) http_req_duration; http_req_failed | k6 отчёт (ссылка/скрин), OBS-1 логи: поле duration p95 за интервал, коды 2xx ≥ 99%

### Плохой результат

Стабильная нагрузка | много запросов | быстро | время | проверить руками

## Запрос

Подготовь улучшенную версию таблицы нагрузочных проверок для сервиса из Практики 1. Сохрани ту же структуру таблицы: Сценарий, Нагрузка и длительность, Допустимый предел, Что измеряем, Подтверждение. Используй стиль как в хорошем примере и избегай размытых формулировок из плохого. Укажи: warm-up/ramp-up, инструмент k6 и конкретные метрики (http_req_duration p(95), http_req_failed, vus, thresholds). Привяжи проверки к правилам CASE.md: API-1, REL-1, OUT-1, OBS-1. Не меняй заголовки и порядок столбцов.

## Что получили

Таблица с уточнёнными сценариями, где каждый сценарий включает k6 метрики и ссылки на подтверждения через логи OBS-1 и артефакты отчётов.

## Что изменили в исходном артефакте

- Файл и раздел:
- Изменение: конкретизированы нагрузочные профили, добавлены методики измерения и подтверждения, явные пороги и привязка к правилам CASE.md.
- Как проверили: соответствие метрикам в `practices/practice_01/problem.md` и `context.md`; структура файла сохранена.
- Что отклонили: расплывчатые формулировки без инструментов и метрик («быстро», «проверить руками»).
